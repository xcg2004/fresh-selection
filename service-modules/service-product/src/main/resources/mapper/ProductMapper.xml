<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xcg.serviceproduct.mapper.ProductMapper">

    <select id="scrollPageByCursor" resultType="com.xcg.freshcommon.domain.product.entity.Product">
        select * from product
        <where>
            status = 1
            <if test="lastId != null">
                and id &lt; #{lastId}
            </if>
            <if test="lastCreateTime != null">
                and create_time &lt; #{lastCreateTime}
            </if>
        </where>
        order by create_time desc, id desc
        limit #{pageSize}
    </select>
    <select id="selectProductBySkuId" resultType="com.xcg.freshcommon.domain.product.entity.Product"
            parameterType="java.lang.Long">
        select * from product p
        inner join product_sku ps on p.id = ps.product_id
        where ps.id = #{skuId} and ps.status = 1 and p.status = 1

    </select>
</mapper>
